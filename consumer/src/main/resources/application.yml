server:
  port: 8083
spring:
  application:
    name: consumer
    spring:
      devtools:
        restart:
          enabled:true

  eureka:
    instance:
      instance-id: ${spring:application:name}:${spring:application:instance_id:${random:value}}
